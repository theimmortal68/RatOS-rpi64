name: Build MainsailOS

on:
  workflow_dispatch:

jobs:
  download-artifact:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout current repository
        uses: actions/checkout@v4

      - name: Install QEMU and dependencies for ARM emulation
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-user-static binfmt-support

      - name: Cache input.img
        uses: actions/cache@v3
        id: cache-input-img
        with:
          path: build/input.img
          key: ${{ runner.os }}-input-img-v2025-05-13
          restore-keys: |
            ${{ runner.os }}-input-img-

      - name: Download and extract RaspiOS Lite 64-Bit if cache miss
        if: steps.cache-input-img.outputs.cache-hit != 'true'
        working-directory: build
        run: |
          curl -L --progress-bar -o raspios-lite.img.xz \
            https://downloads.raspberrypi.com/raspios_lite_arm64/images/raspios_lite_arm64-2025-05-13/2025-05-13-raspios-bookworm-arm64-lite.img.xz
          xz -d raspios-lite.img.xz
          mv raspios-lite.img input.img

      - name: Verify build directory contents
        run: |
          ls -lah build/
          echo "PWD: $(pwd)"
          echo "Listing RatOS-rpi64 directory:"
          ls -l /home/runner/work/RatOS-rpi64/RatOS-rpi64/
          echo "Checking build dir:"
          [ -d "/home/runner/work/RatOS-rpi64/RatOS-rpi64/build" ] && echo "build dir exists" || echo "build dir missing"
          echo "Checking scripts dir:"
          [ -d "/home/runner/work/RatOS-rpi64/RatOS-rpi64/scripts" ] && echo "scripts dir exists" || echo "scripts dir missing"

      - name: Run CustoPiZer
        uses: OctoPrint/CustoPiZer@main
        with:
          workspace: "${{ github.workspace }}/build"
          scripts:  "${{ github.workspace }}/scripts"
          config: "${{ github.workspace }}/config.local"
